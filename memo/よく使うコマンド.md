仮想環境つくる(windows/mac)
python -m venv venv

仮想環境アクティブ(windows)
.\venv\Scripts\Activate.ps1

入れる(win/mac)
pip install openai-whisper

入れる(win)
winget install ffmpeg

vscodeでテキスト折り返し(win)
Alt + Z(再度起動するともどる)
歯車 → Settings → word wrap を on

インストール済のライブラリをテキスト化
pip freeze > requirements.txt

pythonをバージョン指定で入れる
winget install Python.Python.3.11

venvから抜ける
deactivate

現在入ってるpythonのバージョン一覧
py -0
py -0p (詳細)

condaの有効化
conda activate audio_clean
audio_cleanの部分は環境名なので新規のたびに変わる
(audio_clean)となる

1回すんだ場合のgithubへのデプロイ
git add .
git commit -m "update"
git push

condaでrequirements.txtを入れる
pip install -r requirements.txt (pip freezeと間違えないように)

フォルダ構成の保存
function Show-Tree($path, $prefix="") {
    $items = Get-ChildItem $path

    for ($i = 0; $i -lt $items.Count; $i++) {
        $item = $items[$i]
        $isLast = ($i -eq $items.Count - 1)

        $connector = if ($isLast) { "└── " } else { "├── " }
        Write-Output "$prefix$connector$($item.Name)"

        if ($item.PSIsContainer) {
            $newPrefix = if ($isLast) { "$prefix    " } else { "$prefix│   " }
            Show-Tree $item.FullName $newPrefix
        }
    }
}

"src","input","output","confirm","dict" | ForEach-Object {
    Write-Output $_
    Show-Tree $_
    Write-Output ""
} > structure.txt