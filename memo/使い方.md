# 使い方

# 実行環境に入る
conda activate audio_clean

## 準備
`input/001.wav` に音声ファイルを置く。

## 実行
### 004まで実行（whisper → normalize → replace → clean）
python main_to_004.py
出力: `confirm/001_whisper.txt` 〜 `confirm/004_text_clean.txt`

### 最後まで実行（004 → MFA → モーラ → 音声解析 → JSON）
python main.py
出力: `out_json/` にJSON

# 辞書
dict/replace_dict.txt
で置き換わる文字を更新